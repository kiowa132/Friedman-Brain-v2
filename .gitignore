# Obsidian local config (graph layout, workspace state, plugin settings)
# — device-specific, not worth syncing/versioning
.obsidian/

# OS clutter
.DS_Store

# Secrets — the Gemini API key for scripts/gen-image.ps1
scripts/.env

# Generated report images (regenerate from prompts; don't bloat the repo)
properties/*/generated/
